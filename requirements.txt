# Direct dependencies only.
#
# The previous file was a raw `pip freeze` from a conda environment in which
# 86 of 110 entries were local `@ file:///C:/...` build paths, so
# `pip install -r requirements.txt` failed on any other machine.

pandas>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
requests>=2.31
SQLAlchemy>=2.0

# Notebook / exploration
jupyterlab>=4.0
matplotlib>=3.8
seaborn>=0.13

# Tests
pytest>=8.0
